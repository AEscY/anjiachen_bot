# UltimateBot — P0/P1 全量修复说明

## 已修复

### P0 / 交易底座
- 真实成交确认：下单后补取订单，使用 `filled / average / fee / order_id`。
- OKX/CCXT amount precision：统一走 `amount_to_precision()` + 最小/最大数量限制。
- FIFO 持仓账本：每次买入形成独立 lot，多次加仓不再覆盖 entry。
- 加权成本：`entries[symbol]` 从真实 lot 成本计算。
- PnL：使用成交额、买入手续费、卖出手续费计算，不再使用账户 USDT 余额前后差额猜利润。
- Panic Sell：只平机器人账本持仓，不出售账户原有资产。
- 持仓同步：账本与交易所余额不一致时暂停自动交易，不再用当前价格伪造历史入场价。
- 总资金占用：`max_total_allocated_pct` 真正参与每笔开仓检查。
- 单币资金上限：新增订单会受 `max_per_coin_usdt` 剩余预算限制。
- 日亏损熔断：改为基于每日账户权益基准计算，参数读取配置，不再硬编码 8%。
- 账户回撤：风险监控实际维护峰值权益和最大回撤。
- 资金费率套利：当前没有现货+永续双腿，因此默认关闭并拒绝假 Delta Neutral 开仓。
- 实盘安全：`IS_SANDBOX=false` 时必须显式设置 `LIVE_TRADING_CONFIRM=true`。

### P1 / 稳定性与策略层
- WebSocket：ticker 与 orderbook 使用独立重连计数，不再共享 reconnect 状态。
- 不再让 orderbook 重连关闭共享 exchange，避免互相打断。
- `ccxt.pro` 不可用时自动降级到 REST 实时轮询，避免标准 `ccxt` 安装后直接启动失败。
- ATR 无数据时改为 0，不再用与币价无关的固定 `0.01`。
- 多周期数据缺失时交叉因子按中性 50 分处理，不再直接压成 0 分。
- TP/SL 动态波动率函数补齐，并加入上下限和最低盈亏比保护。
- 日计数从“月份内某一天”改为完整日期，避免跨月重置错误。
- 自动交易风险监控任务补齐。
- 缺失的 `_adjust_tp_sl_by_volatility()` 补齐。
- 关闭日志中环境变量名称枚举，减少敏感配置暴露面。
- live 模式无 `ALLOWED_USERS` 时默认拒绝 Telegram 管理命令；模拟盘仍可便于测试。
- SQLite：WAL、索引、运行状态事务、交易详情手续费/订单字段迁移。
- 手续费统计改为真实成交 fee，不再硬编码 0.1%。
- `requirements.txt` 增加合理版本上限，避免未来自动升级破坏生产环境。

## 验证

- 全项目 `compileall`：通过。
- `QuantBot` self-method 调用审计：通过，无缺失私有方法。
- 移动止盈止损路径中市价卖单调用：仅保留一次。
- 已检查关键危险旧模式：账户 USDT 差额 PnL、硬编码 8% 日熔断、错误 precision 算法、环境变量枚举均已移除。

## 尚未做的验证

由于当前运行环境没有安装项目运行依赖，无法在这里真实连接 OKX 模拟盘完成订单级集成测试。因此上线前必须先在 OKX Demo/模拟盘验证：

1. 单次买入 → 真实成交记录。
2. 连续 2–3 次加仓 → FIFO/加权成本。
3. 部分成交 → 账本数量与成交数量一致。
4. 止盈/止损 → PnL 与手续费。
5. 重启机器人 → 状态恢复。
6. Panic Sell → 不动账户原有币。
7. 手工在交易所改变机器人仓位 → 风控暂停。
8. 达到每日亏损/账户回撤 → 自动停止开仓。
9. WebSocket 断线 → 不影响其他订阅。
10. 模拟盘运行至少 24 小时后再考虑实盘。
